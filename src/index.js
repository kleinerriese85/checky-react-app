import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import CheckyInterface from './CheckyInterface';

ReactDOM.render(
  <React.StrictMode>
    <CheckyInterface />
  </React.StrictMode>,
  document.getElementById('root')
);
